# Authors
#
# Additional author profiles can be defined here and referenced from a page's
# front matter via `author: <key>`. When `page.author` is unset, the site-wide
# `author` block in _config.yml is used.
